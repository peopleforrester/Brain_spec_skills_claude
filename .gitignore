# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.*

# Brain Spec workspace (local development data)
.brain-spec/

# Logs
*.log

# Session archives
session_*.md

# Node (legacy, no longer used)
node_modules/
dist/
build/
*.tsbuildinfo
coverage/

# Local Claude Code session history (do not push)
.claude-history/

# Imported context archive from claude.ai migration — local Claude Code context only, never publish (public repo).
claude-ai-context/

# Runtime session artifact from the in-session scheduler (not a project file)
.claude/scheduled_tasks.lock
